/**
 * PocketBase client plugin
 *
 * WHAT IT DOES:
 * Creates one PocketBase client instance and makes it available app-wide.
 *
 * HOW TO USE:
 * const { $pb } = useNuxtApp()
 *
 * TODO: Implement
 * 1. Import PocketBase from 'pocketbase'
 * 2. Create new PocketBase('http://127.0.0.1:8090')
 * 3. return { provide: { pb } }
 */
export default defineNuxtPlugin(() => {
  // TODO: const pb = new PocketBase('http://127.0.0.1:8090')
  // TODO: return { provide: { pb } }
})
